<?php 


#opendir(),readdir(),closedir().


// $dir = ".";

// if(is_dir($dir)){
//     if($d = opendir($dir)){
//         while($file = readdir($d)){
//             echo $file."<br>";
//         }
//     }
// }

// closedir();

/* $dir = "..";

if (is_dir($dir)){
  if ($d = opendir($dir)){
    while (($file = readdir($d))){
      echo "filename:" . $file . "<br>";
    }
    closedir($d);
  }
}
 */


/* $dir = "../";

if (is_dir($dir)){
  if ($d = opendir($dir)){
    while (($file = readdir($d))){
      echo "filename:" . $file . "<br>";
    }
    closedir($d);
  }
} */
?>
?>